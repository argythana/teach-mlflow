source ./.venv/bin/activate
unset PS1
# gh must use the keyring login for this repo; a GH_TOKEN inherited from another
# project's environment would override it with a different GitHub account.
unset GH_TOKEN
